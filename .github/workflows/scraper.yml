name: Scraper
on:
  workflow_dispatch:
  schedule:
    - cron: '0 3 1 * *'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run
        run: echo "Hello World"
